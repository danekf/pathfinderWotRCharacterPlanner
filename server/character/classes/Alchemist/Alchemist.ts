import { BasicCharacter } from "../basicCharacterClass";


//alchemist uses the base character types but implements archetype specific level up, including BaB spells etc
export class Alchemist extends BasicCharacter {

  const baseAttackBonus = [0]

};